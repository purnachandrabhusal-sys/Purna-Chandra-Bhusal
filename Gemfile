# Optional — only needed if Tilak wants to preview the site on his own
# computer with `bundle install && bundle exec jekyll serve`.
# GitHub Pages ignores this file and builds the site automatically.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
